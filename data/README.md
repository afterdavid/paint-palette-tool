# Data

This directory holds the palette dataset and schema.

## Files

- `color-schema.json` — canonical color record schema
- `seed-palette.example.json` — example records only, not the real curated dataset yet

## Next dataset artifact

Create:
- `seed-palette.v1.json`

That file should contain the first real curated palette used to generate the Illustrator swatch library.
